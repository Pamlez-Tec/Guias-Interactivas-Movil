<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISOS NECESARIOS -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuiaPrueba"
        tools:targetApi="31">

        <activity android:name=".WelcomeActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AppTypeSelectionActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".instagram.InstagramPrincipal" android:exported="false" />
        <activity android:name=".instagram.GalleryActivity" />
        <activity android:name=".instagram.PostCreationActivity" />
        <activity android:name=".spotify.SpotifyTutorialActivity" />
        <activity android:name=".spotify.SpotifyTutorialMenuActivity" />
        <activity android:name=".uber.UberTutorialMenuActivity" />
        <activity android:name=".uber.UberInfoTutorialActivity" />
    </application>

</manifest>
